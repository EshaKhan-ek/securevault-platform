name: SecureVault-Husna-Pipeline

permissions:
  actions: read
  contents: read
  security-events: write

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main]

jobs:
  # STAGE 1: SECRET DETECTION
  stage1-secret-scan:
    name: 'Stage 1 - Secret Detection'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gitleaks/gitleaks-action@v2
        continue-on-error: true

  # STAGE 2: SAST ANALYSIS
  stage2-sast-analysis:
    name: 'Stage 2 - SAST Analysis'
    runs-on: ubuntu-latest
    needs: stage1-secret-scan
    strategy:
      matrix:
        language: [python, javascript]
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/autobuild@v4
      - uses: github/codeql-action/analyze@v4

  # STAGE 3: CONTAINER SCAN
  stage3-container-scan:
    name: 'Stage 3 - Container Build & Scan'
    runs-on: ubuntu-latest
    needs: stage2-sast-analysis
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker Image
        run: docker build -f ./docker/auth/Dockerfile -t securevault-auth:${{ github.sha }} .
      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          image-ref: 'securevault-auth:${{ github.sha }}'
          exit-code: '0'

  # STAGE 4: IAC SECURITY SCAN
  stage4-iac-scan:
    name: 'Stage 4 - IaC Security Scan'
    runs-on: ubuntu-latest
    needs: stage3-container-scan
    steps:
      - uses: actions/checkout@v4
      - name: Run Checkov
        uses: bridgecrewio/checkov-action@master
        continue-on-error: true
        with:
          # Directory '.' kar di hai taake folder na milne par crash na ho
          directory: . 
          framework: kubernetes

  # STAGE 6: KUBERNETES DEPLOYMENT (Pehle yeh chalega)
  stage6-deploy:
    name: 'Stage 6 - Deploy to Kubernetes'
    runs-on: ubuntu-latest
    needs: stage4-iac-scan
    # Yahan se 'if: main' hata diya hai taake PR mein bhi chale
    steps:
      - uses: actions/checkout@v4
      - name: Setup Kind Cluster
        uses: helm/kind-action@v1.10.0
      - name: Deploy App
        # Folder check wapis add kar diya hai
        run: |
          if [ -d "k8s" ]; then
            kubectl apply -R -f k8s/
          else
            echo "k8s folder not found, skipping deployment."
          fi

  # STAGE 5: DAST SCAN (Deploy hone ke baad chalega)
  stage5-dast-scan:
    name: 'Stage 5 - DAST Scan'
    runs-on: ubuntu-latest
    needs: stage6-deploy 
    # Yahan se bhi 'if: main' hata diya hai
    steps:
      - name: Wait for API
        run: sleep 30
      - name: Run OWASP ZAP
        continue-on-error: true
        run: |
          docker run -t ghcr.io/zaproxy/zaproxy:stable zap-api-scan.py \
            -t http://api-gateway:8080/openapi.json \
            -f openapi
